[package]
name = "fateen-pos"
version = "1.0.0"
description = "Fateen POS - Offline-first point of sale system"
authors = ["Fateen POS Team"]
license = "MIT"
repository = "https://github.com/FATEEN-POS/test-pos"
edition = "2021"

[build-dependencies]
tauri-build = { version = "1", features = [] }

[dependencies]
serde_json = "1"
serde = { version = "1", features = ["derive"] }
tauri = { version = "1", features = [] }

[features]
